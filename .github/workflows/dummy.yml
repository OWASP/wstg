name: Markdown Lint Check

on:
  pull_request:
    branches:
    - master
    paths-ignore:
    - '**.md'
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Exit with success
        run: exit 0
